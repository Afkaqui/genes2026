# PostgreSQL (en VPS: hostname postgres_db via red postgresql_default)
DATABASE_URL=postgresql://admin:password@postgres_db:5432/genes_intranet_db

# JWT
JWT_SECRET=cambiar_por_un_secreto_seguro_de_al_menos_32_caracteres
JWT_EXPIRES_IN=8h

# Server
PORT=5000
CORS_ORIGIN=http://localhost:3000

# URL publica del frontend (links de verificacion e invitaciones)
PUBLIC_URL=https://www.genesperu.earth

# Envio de correos (Resend) - dominio genesperu.earth verificado
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
MAIL_FROM=GENES Peru <no-reply@genesperu.earth>
